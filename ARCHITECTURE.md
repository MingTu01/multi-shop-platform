# ARCHITECTURE.md - 架构设计文档

## 1. 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        应用层 (apps/)                            │
│  ┌──────────────────┐          ┌─────────────────────────────┐  │
│  │   apps/admin     │          │   apps/store-template       │  │
│  │  管理端应用       │          │   店铺端模板（可插拔）       │  │
│  │  后端 + 前端      │          │   前端（多模板支持）         │  │
│  └────────┬─────────┘          └──────────────┬──────────────┘  │
└───────────┼───────────────────────────────────┼──────────────────┘
            │                                   │
            │         依赖关系                   │
            ▼                                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                      共享包层 (packages/)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────────────────┐   │
│  │  @msp/ui    │  │ @msp/push-  │  │     @msp/shared       │   │
│  │  UI 组件库   │  │   core      │  │  核心共享逻辑包        │   │
│  │  (待实现)    │  │ 推送核心     │  │  类型/权限/工具/API/  │   │
│  │             │  │ (待实现)     │  │  状态/SSE/推送配置     │   │
│  └──────┬──────┘  └──────┬──────┘  └───────────┬───────────┘   │
│         │                │                      │               │
│         └────────────────┼──────────────────────┘               │
│                          │                                      │
│                          ▼                                      │
│              @msp/shared 是底层依赖                             │
└─────────────────────────────────────────────────────────────────┘
```

## 2. 分层架构

### 2.1 分层原则

采用 **共享层 -> 应用层** 两层架构：

```
┌─────────────────────────────────┐
│         应用层 (apps/)           │  ← 可独立部署的完整应用
│  ┌──────────┐  ┌──────────────┐ │
│  │  admin   │  │ store-tpl-1  │ │
│  └──────────┘  └──────────────┘ │
│                  ┌──────────────┐ │
│                  │ store-tpl-2  │ │
│                  └──────────────┘ │
└──────────────┬──────────────────┘
               │ 依赖
               ▼
┌─────────────────────────────────┐
│         共享包层 (packages/)     │  ← 可复用的代码单元
│  ┌────────────────────────────┐ │
│  │        @msp/ui             │ │
│  └──────────────┬─────────────┘ │
│  ┌──────────────▼─────────────┐ │
│  │      @msp/push-core        │ │
│  └──────────────┬─────────────┘ │
│  ┌──────────────▼─────────────┐ │
│  │       @msp/shared          │ │
│  │  (types / role / utils /   │ │
│  │   api / stores / sse /     │ │
│  │   push-config)             │ │
│  └────────────────────────────┘ │
└─────────────────────────────────┘
```

### 2.2 各层职责

| 层级 | 职责 | 包含内容 |
|------|------|---------|
| 应用层 (apps/) | 可独立部署的完整应用 | 管理端、店铺端模板（多个） |
| 共享包层 (packages/) | 可复用的代码单元，不直接部署 | shared、ui、push-core |

### 2.3 依赖方向

- 上层依赖下层，下层不依赖上层
- 同层之间：ui -> shared，push-core -> shared
- 应用层可以依赖任意共享包
- 共享包之间禁止循环依赖

## 3. 管理端和店铺端的职责划分

### 3.1 管理端 (apps/admin)

**面向角色**：系统管理员 (ADMIN)、店铺管理员 (STORE_ADMIN)

**核心职责**：
- 多店铺统一管理（店铺增删改查、店铺配置）
- 用户管理（员工账号、角色分配、权限控制）
- 系统配置（全局设置、推送配置、升级管理）
- 数据汇总（全局仪表盘、跨店报表、经营分析）
- 审批流（异常审核、工资确认、分红审批）
- 系统监控（操作日志、系统告警、升级管理）

**技术特点**：
- 包含完整的后端服务（Express + SQLite）
- 包含管理端前端
- 提供 API 供店铺端调用
- 数据库统一存储（所有店铺数据在一个库中）

### 3.2 店铺端 (apps/store-template)

**面向角色**：店长 (MANAGER)、员工 (STAFF)、股东 (SHAREHOLDER)

**核心职责**：
- 单店铺日常运营（记账、库存、排班、工资）
- 店铺级数据查看（报表、流水、分红）
- 个人信息管理（密码修改、推送设置）
- 消息通知查看

**技术特点**：
- 纯前端应用（通过 API 与管理端后端通信）
- 可插拔模板设计，支持多套模板
- 每个模板可针对不同行业/规模定制
- 通过 API 进店通道与后端交互

### 3.3 职责边界对比

| 功能域 | 管理端 | 店铺端 |
|--------|--------|--------|
| 店铺管理 | ✅ 创建/编辑/删除/配置 | ❌ 仅查看本店信息 |
| 用户管理 | ✅ 全店用户管理 | ❌ 仅个人信息 |
| 记账录入 | ✅ 跨店查看/录入 | ✅ 本店录入/查看 |
| 库存管理 | ✅ 跨店查看 | ✅ 本店操作 |
| 排班管理 | ✅ 跨店查看 | ✅ 本店操作 |
| 工资管理 | ✅ 配置/审批 | ✅ 查看/确认 |
| 分红管理 | ✅ 配置/发放 | ✅ 查看 |
| 系统设置 | ✅ 全局配置 | ❌ |
| 升级管理 | ✅ | ❌ |
| 操作日志 | ✅ 全店 | ✅ 本店 |

## 4. 数据流向

### 4.1 前端 -> 后端 -> 数据库

```
┌──────────────┐    HTTP/HTTPS    ┌──────────────┐    better-sqlite3   ┌──────────────┐
│  React 前端   │ ──────────────► │  Express 后端 │ ──────────────────► │  SQLite DB   │
│  (Vite)      │ ◄────────────── │  (Node.js)   │ ◄────────────────── │  (store.db)  │
└──────────────┘   JSON Response  └──────────────┘   同步查询/写入       └──────────────┘
       │                                │
       │                                │
       ▼                                ▼
  zustand state                     业务逻辑层
  (client-side)                    (services/repos)
```

### 4.2 详细数据流

1. **前端发起请求**
   - 通过 `@msp/shared/api` 的 `api.get/post/put/del` 发起请求
   - 自动附带 cookie 凭据（credentials: 'include'）
   - 命中 LRU 缓存的 GET 请求直接返回

2. **后端处理**
   - Express 路由层接收请求
   - 中间件鉴权（JWT + cookie）
   - 业务逻辑层处理
   - better-sqlite3 同步操作数据库

3. **数据库**
   - 单文件 SQLite (store.db)
   - 所有店铺数据存在同一个库中
   - 通过 store_id 区分不同店铺数据

4. **响应返回**
   - JSON 格式响应
   - 前端更新 zustand store
   - SSE 推送给其他相关客户端

### 4.3 缓存层级

```
┌─────────────────────────────────────────────────────────┐
│  浏览器端                                                │
│  ┌─────────────────┐                                    │
│  │  zustand store  │  ← 内存状态，跨组件共享             │
│  └────────┬────────┘                                    │
│           │                                             │
│  ┌────────▼────────┐                                    │
│  │  API LRU Cache  │  ← 200 条，按 URL 维度 TTL         │
│  └────────┬────────┘                                    │
│           │                                             │
└───────────┼─────────────────────────────────────────────┘
            │ HTTP 请求
            ▼
┌─────────────────────────────────────────────────────────┐
│  服务端                                                  │
│  ┌─────────────────┐                                    │
│  │  Express 内存    │  ← 可选，热点数据缓存              │
│  └────────┬────────┘                                    │
│           │                                             │
│  ┌────────▼────────┐                                    │
│  │  SQLite 数据库   │  ← 持久化存储                     │
│  └─────────────────┘                                    │
└─────────────────────────────────────────────────────────┘
```

## 5. 推送通知架构

### 5.1 方案 A：按角色区分 content

当前采用方案：**按角色区分推送内容**

```
                    ┌───────────────────┐
                    │   业务事件触发     │
                    │  (记账/入库/排班等)│
                    └─────────┬─────────┘
                              │
                              ▼
                    ┌───────────────────┐
                    │  推送触发层        │
                    │  (push-trigger)   │
                    └─────────┬─────────┘
                              │
                              ▼
                    ┌───────────────────┐
                    │  角色权限过滤      │
                    │  ROLE_ALLOWED_TYPES│
                    └─────────┬─────────┘
                              │
            ┌─────────────────┼─────────────────┐
            ▼                 ▼                 ▼
     ┌────────────┐   ┌────────────┐   ┌────────────┐
     │  ADMIN     │   │ STORE_     │   │  MANAGER   │
     │  (全部)     │   │  ADMIN     │   │  (部分)    │
     └──────┬─────┘   └──────┬─────┘   └──────┬─────┘
            │                │                │
            ▼                ▼                ▼
     ┌──────────────────────────────────────────────┐
     │           推送渠道层                           │
     │  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
     │  │ PushPlus │  │  企业微信  │  │ 爱语飞飞  │    │
     │  └──────────┘  └──────────┘  └──────────┘    │
     └──────────────────────────────────────────────┘
```

### 5.2 推送类型与角色矩阵

| 推送类型 | ADMIN | STORE_ADMIN | MANAGER | STAFF | SHAREHOLDER |
|---------|-------|-------------|---------|-------|-------------|
| entry (记账) | ✅ | ✅ | ✅ | ❌ | ❌ |
| payroll (工资) | ✅ | ✅ | ✅ | ✅ | ❌ |
| dividend (分红) | ✅ | ✅ | ❌ | ❌ | ✅ |
| inventory (库存) | ✅ | ✅ | ✅ | ❌ | ❌ |
| shift (排班) | ✅ | ✅ | ✅ | ❌ | ❌ |
| health_cert (健康证) | ✅ | ✅ | ✅ | ✅ | ❌ |
| staff (员工) | ✅ | ✅ | ✅ | ❌ | ❌ |
| store (门店) | ✅ | ✅ | ✅ | ❌ | ✅ |
| purchase (进货) | ✅ | ✅ | ✅ | ❌ | ❌ |
| salary_confirm (工资确认) | ✅ | ✅ | ❌ | ✅ | ❌ |
| inventory_alert (库存预警) | ✅ | ✅ | ✅ | ❌ | ❌ |
| store_alert (门店预警) | ✅ | ❌ | ❌ | ❌ | ✅ |
| daily_report (日报) | ✅ | ✅ | ✅ | ❌ | ❌ |
| weekly_report (周报) | ✅ | ✅ | ❌ | ❌ | ❌ |
| monthly_report (月报) | ✅ | ✅ | ❌ | ❌ | ❌ |
| review_reminder (审核提醒) | ✅ | ✅ | ❌ | ❌ | ❌ |
| alert (系统告警) | ✅ | ❌ | ❌ | ❌ | ❌ |

### 5.3 推送渠道

| 渠道 | 说明 | 权限 |
|------|------|------|
| PushPlus | 微信公众号推送 | 所有角色 |
| 企业微信 | 企业微信应用消息 | 仅管理员 |
| 爱语飞飞 | 微信公众号推送 | 所有角色 |

## 6. SSE 多 Tab 架构

### 6.1 Leader 选举机制

采用 **localStorage + BroadcastChannel** 实现多 Tab leader 选举：

```
┌─────────────────────────────────────────────────────────────┐
│  浏览器多 Tab                                                │
│                                                              │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐              │
│  │  Tab 1   │    │  Tab 2   │    │  Tab 3   │              │
│  │ (Leader) │    │(Follower)│    │(Follower)│              │
│  └────┬─────┘    └────┬─────┘    └────┬─────┘              │
│       │                │                │                    │
│       │ SSE 连接       │ 仅监听广播     │ 仅监听广播          │
│       ▼                ▼                ▼                    │
│  ┌──────────────────────────────────────────────┐           │
│  │         BroadcastChannel ('msl-sse')         │           │
│  └──────────────────────────────────────────────┘           │
│                                                              │
│  ┌──────────────────────────────────────────────┐           │
│  │  localStorage                                 │           │
│  │  - msl_sse_leader (tabId)                    │           │
│  │  - msl_sse_heartbeat (timestamp)             │           │
│  └──────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 选举流程

1. **启动时**
   - 检查 `localStorage.msl_sse_heartbeat` 是否在 TTL（5s）内
   - 如果 leader 存活，当前 Tab 成为 follower
   - 如果 leader 不存活，尝试成为 leader

2. **Leader 心跳**
   - Leader Tab 每 2s 更新一次 `msl_sse_heartbeat`
   - Follower Tab 通过 storage 事件监听 leader 状态

3. **Leader 宕机接管**
   - Leader Tab 关闭时，通过 beforeunload 清理 leader 标识
   - Follower 监听到 leader key 被清除后，延迟 100ms 后尝试竞选
   - 首个成功写入 leader key 的 Tab 成为新 leader

4. **消息广播**
   - Leader Tab 收到 SSE 消息后，通过 BroadcastChannel 广播给所有 Tab
   - Follower Tab 接收广播消息，更新本地状态

### 6.3 SSE 事件类型

| 事件类型 | 说明 | 处理动作 |
|---------|------|---------|
| message | 默认消息 | 解析 data 并处理 |
| data-change | 数据变更通知 | bump 版本号 + 清除相关缓存 |
| system | 系统事件 | 如 server-ready 等 |
| heartbeat | 心跳 | 忽略，保持连接活跃 |

### 6.4 重连策略

- 初始延迟：3000ms
- 指数退避：每次失败延迟翻倍
- 最大延迟：60000ms
- 最大重试次数：20 次
- 登录成功后主动重连

## 7. 部署架构

### 7.1 Docker + volume mount

```
┌─────────────────────────────────────────────────────────┐
│                   宿主机                                  │
│  ┌──────────────────────────────────────────────────┐   │
│  │              Docker 容器                          │   │
│  │                                                  │   │
│  │  /app/                                          │   │
│  │  ├── src/           (COPY 到镜像，不可变)        │   │
│  │  ├── public/        (COPY 到镜像，不可变)        │   │
│  │  ├── package.json   (COPY 到镜像，不可变)        │   │
│  │  │                                                 │   │
│  │  │  Volume Mounts (持久化，直接操作宿主机)        │   │
│  │  ├── data/          → /opt/msp/data              │   │
│  │  │   └── store.db   (SQLite 数据库)              │   │
│  │  ├── uploads/       → /opt/msp/uploads           │   │
│  │  │   └── (用户上传文件)                           │   │
│  │  └── public/web-dist → /opt/msp/web-dist         │   │
│  │      └── (前端构建产物)                           │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  /opt/msp/          (宿主机持久化目录)                   │
│  ├── data/                                              │
│  ├── uploads/                                           │
│  └── web-dist/                                          │
└─────────────────────────────────────────────────────────┘
```

### 7.2 src-seed 机制

用于初始化/升级时将镜像内的代码种子复制到 volume：

```
启动流程：
1. 容器启动，检查 volume 中的 src/ 是否存在或版本落后
2. 若需要更新，将镜像内的 src-seed/ 复制到 volume 的 src/
3. 启动 Node 服务，读取 volume 中的 src/

注意事项：
- 升级操作必须原子化（先复制到临时目录，再替换）
- 数据库迁移需要同步执行
- 重大版本更新需要重建镜像（docker-compose up -d --build）
```

### 7.3 端口与网络

- 管理端后端：3001 端口
- 管理端前端：由后端静态文件服务提供
- 店铺端：可独立部署，或由后端统一提供
- 反向代理：Nginx（可选）

## 8. 安全架构

### 8.1 认证体系：JWT + Cookie

```
┌──────────────┐                                  ┌──────────────┐
│   浏览器      │                                  │   服务端      │
└──────┬───────┘                                  └──────┬───────┘
       │  1. POST /api/auth/login                        │
       │  { username, password }                         │
       │───────────────────────────────────────────────►│
       │                                                 │
       │  2. 验证用户，生成 JWT                           │
       │     Set-Cookie: token=xxx; HttpOnly;            │
       │                 Secure; SameSite=Strict         │
       │◄───────────────────────────────────────────────│
       │                                                 │
       │  3. 后续请求自动携带 Cookie                      │
       │  Cookie: token=xxx                              │
       │───────────────────────────────────────────────►│
       │                                                 │
       │  4. 中间件验证 JWT，解析用户信息                  │
       │     返回业务数据                                 │
       │◄───────────────────────────────────────────────│
```

**安全特性**：
- HttpOnly Cookie：防止 XSS 窃取
- Secure：仅 HTTPS 传输
- SameSite=Strict：防止 CSRF
- 401 自动重定向登录页（防重入保护）

### 8.2 SSRF 防护

针对 Webhook URL 做 SSRF 防护，防止用户配置恶意内网地址：

**同步校验**（`validateWebhookUrl`）：
- 协议校验：仅允许 http/https
- 字面量 IP 校验：拒绝内网 IP（10.x、172.16-31.x、192.168.x、127.x、169.254.x）
- IPv6 内网：fc/fd 开头、fe80 开头、::1
- 云元数据地址黑名单：169.254.169.254 等

**异步校验**（`validateWebhookUrlAsync`）：
- 在同步校验基础上
- DNS 解析域名，检查解析后的 IP 是否为内网地址
- 同时校验 IPv4 和 IPv6

### 8.3 CSP（内容安全策略）

| 指令 | 策略 | 说明 |
|------|------|------|
| default-src | 'self' | 默认同源 |
| script-src | 'self' | 仅本地脚本 |
| style-src | 'self' 'unsafe-inline' | 允许内联样式 |
| img-src | 'self' data: blob: | 允许 data URI 和 blob |
| connect-src | 'self' | API/SSE 仅同源 |
| frame-ancestors | 'none' | 禁止嵌入 iframe |

### 8.4 其他安全措施

| 措施 | 说明 |
|------|------|
| 密码哈希 | bcrypt，cost factor 10+ |
| SQL 注入防护 | better-sqlite3 预编译语句 + 参数化查询 |
| XSS 防护 | React 默认转义 + CSP |
| CSRF 防护 | SameSite Cookie + 自定义请求头 |
| 速率限制 | 登录接口限流，防止暴力破解 |
| 文件上传 | 类型校验 + 大小限制 + 随机文件名 |
| 错误处理 | 生产环境不返回堆栈信息 |
